export const informalGreeting = (person) => {
    console.log(`Hi, ${person} Okezie`);
}

export const formalGreeting = (person) => {
    console.log(`Hello, ${person} Okezie`); 
}

const greeting = (name) => {
    console.log(`Howdy! ${name}`)
}

export default greeting;